import React from "react";

/////////////////////예시
const Button = ({ color }) => {
    return (
        <div>
            <button style={{ color }}>버튼</button>
        </div>
    );
};

export default Button;
